# 수능수학 적응형 학습 서비스 - 통합 설계

## 📋 프로젝트 개요

수능 수학 문제를 카카오톡을 통해 매일 제공하고, 사용자의 실력에 맞춰 난이도를 조정하는 적응형 학습 서비스

### 핵심 가치
- 🎯 **적응형 난이도**: 사용자 실력에 맞춘 문제 제공
- 📱 **카카오톡 연동**: 익숙한 플랫폼에서 학습
- 🎨 **깔끔한 UI/UX**: 문제 이미지 + 버튼 기반 인터랙션
- 📊 **학습 추적**: 풀이 기록 및 통계

---

## 🏗️ 시스템 아키텍처

### 전체 구조도
```
┌─────────────────────────────────────────────────────────┐
│                     사용자 (학생)                          │
│                    ↓ 카카오톡 ↓                          │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│              FastAPI 웹 서버 (server/)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────────┐ │
│  │ Auth Routes  │  │ Problem      │  │ Message       │ │
│  │ (카카오 로그인)│  │ Routes       │  │ Routes        │ │
│  │              │  │ (문제 관리)   │  │ (메시지 발송)  │ │
│  └──────────────┘  └──────────────┘  └───────────────┘ │
└─────────────────────────────────────────────────────────┘
           ↓                    ↓                  ↓
┌──────────────────┐  ┌──────────────────┐  ┌─────────────┐
│   Supabase DB    │  │ Supabase Storage │  │ Kakao API   │
│  (문제, 사용자,   │  │  (문제 이미지)    │  │ (메시지 발송)│
│   풀이 기록)      │  │                  │  │             │
└──────────────────┘  └──────────────────┘  └─────────────┘
```

---

## 🎯 핵심 기능

### 1. 사용자 인증 (OAuth 2.0)
**구현 위치**: `server/auth.py`, `server/users.py`

```python
# 카카오 로그인 플로우
GET /auth/kakao
  → 카카오 로그인 페이지로 리다이렉트
  → 사용자 로그인
  → callback with code
GET /auth/kakao/callback?code=...
  → 토큰 교환
  → 사용자 정보 저장
  → 세션 생성
  → 대시보드로 리다이렉트
```

**데이터 구조** (`users` 테이블):
- `id`: UUID (primary key)
- `kakao_id`: 카카오 사용자 ID
- `access_token`: 카카오 액세스 토큰
- `current_level`: 현재 난이도 레벨 (1-5)
- `total_problems_solved`: 총 푼 문제 수
- `correct_count`: 맞춘 문제 수
- `subscription_type`: 구독 유형 (free/premium)

### 2. 문제 관리 시스템

#### 2.1 Admin 페이지
**URL**: `http://localhost:8000/problem/admin`
**기능**:
- 문제 목록 조회 (필터링: 상태, 연도, 시험)
- 문제 이미지 크롭 (React 모달)
- 문제 발송 (Send 버튼)
- 통계 대시보드

**구현 파일**:
- `server/problem_routes.py`: 백엔드 API
- `server/static/crop-modal.jsx`: 이미지 크롭 UI
- `server/static/shared-crop-components.jsx`: 재사용 컴포넌트

#### 2.2 문제 뷰어 (Problem Viewer)
**URL**: `https://[ngrok-domain]/problem/view/{problem_id}`
**기능**:
- 문제 이미지 표시 (줌 인/아웃)
- 답안 입력/선택
- 정답 확인
- 힌트 표시 (3단계)

**핵심 코드** (`server/problem_routes.py:689-795`):
```python
@router.get("/view/{problem_id}", response_class=HTMLResponse)
async def view_problem(request: Request, problem_id: str):
    # 문제 데이터 로드
    problem = supabase.get_problem(problem_id)

    # HTML 생성 (Pure HTML/CSS/JS - React 없음)
    html = f"""
    <!DOCTYPE html>
    <html lang="ko">
    <head>...</head>
    <body>
        <div class="debug">DEBUG: Page loaded...</div>
        <div class="header">{year} {exam} #{number}</div>
        <div class="image-container">
            <img src="{image_url}" onerror="...">
        </div>
        <div class="answer-section">
            <input type="text" id="answerInput">
        </div>
        <button onclick="submitAnswer()">정답 확인</button>
        <script>
            function submitAnswer() {{
                // 클라이언트 측 정답 확인
                const isCorrect = userAnswer === correctAnswer;
                // 결과 표시
            }}
        </script>
    </body>
    </html>
    """
    return HTMLResponse(content=html)
```

**특징**:
- ✅ 카카오톡 웹뷰 호환 (Pure HTML/CSS/JS)
- ✅ 디버그 배너 (문제 ID, 로드 시간)
- ✅ 이미지 에러 핸들링
- ✅ 줌 컨트롤 (확대/축소/리셋)
- ✅ 키보드 입력 지원 (Enter → 제출)

### 3. 카카오톡 메시지 발송

#### 3.1 메시지 구조 (Feed Template)
**구현 위치**: `server/kakao_message.py:163-245`

```python
def send_math_problem(
    access_token: str,
    problem_id: str,
    problem_image_url: str,
    year: int,
    exam: str,
    number: int,
    difficulty: str,
    unit: str,
    button_title: str,
    button_url: str
):
    # 제목 생성
    title = f"🎓 {year} 수능 {number}번"

    # 설명 생성
    desc = f"⭐⭐⭐ {difficulty} · 📚 {unit}\n💡 버튼을 눌러 문제를 풀어보세요!"

    # Feed 템플릿
    template = {
        "object_type": "feed",
        "content": {
            "title": title,
            "description": desc,
            "image_url": problem_image_url + f"?t={timestamp}",
            "link": {...}
        },
        "buttons": [{
            "title": "🎯 문제 풀기",
            "link": {
                "web_url": button_url,
                "mobile_web_url": button_url
            }
        }]
    }
```

**메시지 레이아웃**:
```
┌─────────────────────────────┐
│ 🎓 2026 수능 3번              │  ← 제목
│ ⭐⭐⭐ 3점 · 📚 미분          │  ← 메타데이터
├─────────────────────────────┤
│                             │
│     [문제 이미지 미리보기]     │  ← 문제 썸네일
│                             │
├─────────────────────────────┤
│ 💡 버튼을 눌러 문제를 풀어보세요! │ ← CTA
└─────────────────────────────┘
      [🎯 문제 풀기] 버튼         ← 웹뷰 링크
```

#### 3.2 발송 플로우
```
1. Admin 페이지에서 Send 버튼 클릭
   POST /problem/send { problem_id }

2. 서버에서 문제 데이터 로드
   - Supabase에서 문제 정보 조회
   - 이미지 URL 추출

3. 카카오톡 메시지 생성 및 발송
   - 메시지 템플릿 구성
   - 캐시 버스팅 (timestamp 추가)
   - KakaoTalk Message API 호출

4. 문제 상태 업데이트
   - status: ready → sent
   - user_problems 테이블에 기록
```

---

## 📊 데이터베이스 스키마

### 핵심 테이블

#### `problems` (문제 정보)
```sql
CREATE TABLE problems (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    problem_id VARCHAR(50) UNIQUE NOT NULL,  -- 예: 2026_CSAT_Q03
    year INT,
    exam VARCHAR(20),  -- CSAT, KICE6, KICE9
    question_no INT,
    score INT,  -- 2, 3, 4점
    difficulty INT,  -- 1-5 (AI 분석 기반)
    unit VARCHAR(100),  -- 미분, 적분, 수열 등
    image_url TEXT,  -- Supabase Storage URL
    answer VARCHAR(50),
    answer_verified VARCHAR(50),
    solution TEXT,
    hint1 TEXT,
    hint2 TEXT,
    hint3 TEXT,
    is_multiple_choice BOOLEAN DEFAULT true,
    choices JSONB,  -- ["1", "2", "3", "4", "5"]
    status VARCHAR(20) DEFAULT 'ready',  -- ready, sent
    created_at TIMESTAMP DEFAULT NOW()
);
```

#### `users` (사용자 정보)
```sql
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    kakao_id VARCHAR(50) UNIQUE NOT NULL,
    nickname VARCHAR(100),
    email VARCHAR(255),
    access_token TEXT,
    refresh_token TEXT,
    token_expires_at TIMESTAMP,
    current_level INT DEFAULT 3,  -- 1-5
    current_score_level INT DEFAULT 3,
    preferred_subject VARCHAR(50),
    subscription_type VARCHAR(20) DEFAULT 'free',
    total_problems_solved INT DEFAULT 0,
    correct_count INT DEFAULT 0,
    consecutive_correct INT DEFAULT 0,
    consecutive_wrong INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    last_login_at TIMESTAMP
);
```

#### `user_problems` (풀이 기록)
```sql
CREATE TABLE user_problems (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    user_id UUID REFERENCES users(id),
    problem_id VARCHAR(50) REFERENCES problems(problem_id),
    user_answer VARCHAR(50),
    is_correct BOOLEAN,
    hint_used INT DEFAULT 0,  -- 0-3
    time_spent INT,  -- 초 단위
    solved_at TIMESTAMP DEFAULT NOW()
);
```

---

## 🔧 기술 스택

### Backend
- **Framework**: FastAPI 0.109+
- **Python**: 3.11+
- **ASGI Server**: Uvicorn (hot reload)
- **Database**: Supabase PostgreSQL
- **Storage**: Supabase Storage
- **Authentication**: OAuth 2.0 (Kakao)

### Frontend (Minimal)
- **Admin**: HTML + Vanilla JS + React CDN
- **Problem Viewer**: Pure HTML/CSS/JavaScript
- **Styling**: Inline CSS (no framework)

### External APIs
- **Kakao Login**: OAuth 2.0 (`kauth.kakao.com`)
- **Kakao Message**: KakaoTalk 나에게 보내기 API (`kapi.kakao.com/v2/api/talk/memo/default/send`)

### DevOps
- **Tunneling**: ngrok (localhost → public URL)
- **Version Control**: Git
- **Environment**: `.env` file (python-dotenv)

---

## 🚀 배포 및 운영

### 로컬 개발 환경
```bash
# 1. 환경 변수 설정
cp .env.example .env
# .env 파일 편집 (Supabase, Kakao 키 입력)

# 2. 의존성 설치
pip install -r requirements.txt

# 3. 서버 실행
python -m uvicorn server.main:app --host 0.0.0.0 --port 8000 --reload

# 4. ngrok 터널링
ngrok http 8000
# → https://xxx.ngrok-free.app
```

### 환경 변수 (.env)
```bash
# Supabase
SUPABASE_URL=https://[project].supabase.co
SUPABASE_KEY=[anon_key]
SUPABASE_SERVICE_KEY=[service_role_key]

# Kakao
KAKAO_REST_API_KEY=[rest_api_key]
KAKAO_CLIENT_SECRET=[client_secret]
KAKAO_REDIRECT_URI=http://localhost:8000/auth/kakao/callback

# Server
BASE_URL=https://[ngrok-domain].ngrok-free.dev
```

### 카카오 개발자 콘솔 설정
1. **앱 생성**: https://developers.kakao.com
2. **Redirect URI 등록**:
   - `http://localhost:8000/auth/kakao/callback`
3. **웹 도메인 등록** (제품 링크 관리):
   - `https://[ngrok-domain].ngrok-free.dev`
4. **동의 항목 설정**:
   - 닉네임 (필수)
   - 이메일 (선택)

---

## 📱 사용자 플로우

### 전체 워크플로우
```
1. 사용자 로그인
   http://localhost:8000/
   → [Kakao 로그인] 버튼
   → 카카오 인증
   → 대시보드

2. Admin이 문제 발송
   /problem/admin
   → [Send] 버튼 클릭
   → 카카오톡 메시지 발송

3. 사용자가 카카오톡 확인
   카카오톡 앱
   → 메시지 수신
   → 문제 이미지 미리보기
   → [🎯 문제 풀기] 버튼

4. 웹뷰에서 문제 풀이
   https://[ngrok]/problem/view/2026_CSAT_Q03
   → 문제 이미지 확인 (줌 가능)
   → 답 입력
   → [정답 확인] 버튼
   → 즉시 피드백 (정답/오답)

5. (미래) 서버 연동
   → POST /problem/submit
   → 풀이 기록 저장
   → 난이도 조정
   → 다음 문제 추천
```

---

## 🎨 UI/UX 설계

### 디자인 원칙
1. **모바일 퍼스트**: 카카오톡 웹뷰 최적화
2. **최소 클릭**: 1-2번 클릭으로 핵심 액션
3. **즉각 피드백**: 로딩 상태, 성공/실패 메시지
4. **깔끔한 레이아웃**: 그라데이션, 둥근 모서리, 그림자

### 색상 팔레트
```css
/* Primary */
--primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* Success */
--success-gradient: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);

/* Error */
--error-gradient: linear-gradient(135deg, #eb3349 0%, #f45c43 100%);

/* Neutral */
--bg-gray: #f5f5f5;
--text-dark: #333;
--text-gray: #666;
```

---

## 📈 향후 개발 로드맵

### Phase 1: 기본 기능 (완료 ✅)
- [x] 카카오 로그인
- [x] Admin 페이지
- [x] 문제 뷰어
- [x] 카카오톡 메시지 발송
- [x] 이미지 크롭 도구

### Phase 2: 서버 연동 (다음 단계)
- [ ] 서버 측 답안 제출 API
- [ ] 풀이 기록 저장
- [ ] 사용자 통계 업데이트
- [ ] 난이도 조정 알고리즘

### Phase 3: 고급 기능
- [ ] 힌트 시스템 (3단계)
- [ ] 객관식 문제 지원
- [ ] 상세 풀이 표시
- [ ] 학습 대시보드

### Phase 4: 프로덕션 준비
- [ ] 도메인 구매 및 SSL 설정
- [ ] Supabase RLS (Row Level Security)
- [ ] 에러 모니터링 (Sentry)
- [ ] 성능 최적화
- [ ] 사용자 피드백 수집

---

## 🐛 알려진 이슈

### 1. 카드 생성 실패
**증상**: `[Card Upload Failed] Bucket not found`
**원인**: Supabase Storage 버킷 권한 또는 경로 문제
**해결**: 현재는 원본 이미지 사용 (카드 생성 비활성화)

### 2. ngrok 경고 페이지
**증상**: ngrok 무료 버전 사용 시 "You are about to visit..." 경고
**원인**: ngrok 정책
**해결**: 사용자가 "Visit Site" 클릭 필요

### 3. StatReload 인코딩 에러
**증상**: Windows 콘솔에서 한글 출력 시 cp949 에러
**원인**: Windows 기본 인코딩
**해결**: 로그 메시지에서 한글/이모지 제거

---

## 📞 지원 및 문의

### 문서
- **설정 가이드**: `SETUP_GUIDE.md`
- **초보자 가이드**: `BEGINNER_GUIDE.md`
- **파이프라인 가이드**: `PIPELINE_GUIDE.md`

### 디버깅
```bash
# 서버 로그 확인
tail -f [task_output_file]

# Supabase 연결 테스트
python -c "from src.supabase_service import SupabaseService; s = SupabaseService(); print('OK')"

# 카카오 토큰 확인
# Admin 페이지 → Network 탭 → /problem/send 요청 확인
```

---

## 📝 라이선스

이 프로젝트는 교육 목적으로 개발되었습니다.

---

**마지막 업데이트**: 2026-02-06
**버전**: 1.0.0
